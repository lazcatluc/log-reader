# log-reader

Prerequisites: Java 8, Maven 3.

How to run:
1. mvn clean install
2. java -jar target\log-reader.jar <lines>

A file "generated.log" will be generated inside the target folder and subsequently parsed
